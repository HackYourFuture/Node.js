const help = function printHelp() {
  console.log(`

    To - Do App
    Options:

    list          :         read all to - dos
    remove[to -do]:         remove to -do
    add[to -do]   :         add to -do
    help          :         show this help text
        `);
}

module.exports = help;